#!/bin/bash

[[ -d core ]] || git clone https://github.com/hoobs-org/core.git
[[ -d connect ]] || git clone https://github.com/hoobs-org/connect.git

mkdir builds
yarn install

cd ./core
git checkout 3.3.5
npm install --ignore-scripts

cd ../connect
git checkout 3.3.5
yarn install

cd ../
