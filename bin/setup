#!/bin/bash

[[ -d hoobsd ]] || git clone https://github.com/hoobs-org/hoobsd.git
[[ -d cli ]] || git clone https://github.com/hoobs-org/cli.git
[[ -d gui ]] || https://github.com/hoobs-org/gui.git

cd ./hoobsd
yarn install

cd ../cli
yarn install
yarn build

cd ../gui
yarn install

cd ../
